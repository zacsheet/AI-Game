# GameBot 后续开发计划

本文档整理后续我建议继续优化和开发的方向，目标是让 GameBot 从“可用原型”逐步变成“稳定、直观、可长时间使用”的自动化工具。

## 总体方向

GameBot 当前核心能力已经跑通：图片匹配、坐标动作、录制、蓝图分支、任务队列、日志和热键都有了。后续重点不应只继续堆功能，而应优先增强稳定性、可维护性和编辑体验。

我的建议优先级：

1. 稳定性与安全。
2. 调度逻辑可测试。
3. 编辑器拆分和蓝图交互增强。
4. 性能优化。
5. 长时间运行体验。

## P0：稳定性与安全

### 1. 日志落盘

当前日志只显示在 UI 列表里，重启后会丢失。建议同时写入本地日志文件：

- 路径示例：`logs/gamebot_YYYY-MM-DD.log`
- 内容包括时间、日志级别、任务名、规则名、动作结果。
- UI 中保留“复制”和“清空”，但清空只清 UI，不删除文件。

### 2. 热键状态显式提示

全局热键可能被系统权限、杀毒软件或反作弊拦截。建议：

- 启动时在状态栏显示热键是否注册成功。
- 如果注册失败，在界面顶部显示明显提示。
- 允许用户在配置里改热键。

### 3. 窗口检查异常更保守

如果窗口状态检测发生异常，不建议默认继续执行。更安全的策略：

- 检测异常时返回不可用。
- 日志中提示“窗口检查失败，本轮跳过”。
- 提供一个高级开关，允许用户明确跳过窗口检查。

### 4. 动作安全间隔

当前动作执行追求快速，但误操作时风险较高。建议：

- 增加全局动作最小间隔设置。
- 默认给点击、按键、拖拽之间保留短暂间隔。
- 高级用户可手动设置为 0。

## P1：调度器和规则引擎优化

### 1. 明确任务队列模型

当前逻辑应固化为：

- 按任务列表顺序执行。
- 当前任务不限次数时，一直执行当前任务。
- 当前任务达到次数上限后，进入下一个启用任务。
- 所有任务完成后，停止本轮运行。

后续可以在 UI 上明确显示“当前任务 / 已执行次数 / 队列进度”。

### 2. 将规则分支改为显式状态机

当前规则分支是 `while + rule_positions` 的跳转模型，功能可用，但复杂规则会难以追踪。建议抽象为：

- `RuleExecutionContext`
- `RuleStepResult`
- `next_rule_id`
- `end_task`

这样更方便测试，也更容易以后支持更多节点类型。

### 3. 实现或移除 ExecuteMode

模型中已有：

- `sequence`
- `parallel`
- `priority`

但当前调度实际按顺序执行。建议二选一：

- 短期：UI 上隐藏 `parallel` 和 `priority`，避免误解。
- 中期：实现 `priority`，按规则优先级执行。
- 长期：谨慎实现 `parallel`，因为自动化输入并发容易产生冲突。

## P1：测试覆盖

建议先补最关键的单元测试，而不是追求高覆盖率。

### 1. 配置加载测试

覆盖：

- 旧 JSON 没有新字段时能正常加载。
- `max_executions` 默认值为 0。
- 动作固定坐标字段兼容旧配置。

### 2. 调度器测试

覆盖：

- 任务 1 执行 1 次后进入任务 2。
- 任务设置不限次数时不会进入后续任务。
- 连续失败达到上限后进入下一个任务。
- 规则分支跳到指定规则。
- 规则分支结束任务。

### 3. 录制器测试

覆盖：

- 短距离鼠标按下松开记录 click。
- 长距离鼠标按下移动松开记录 drag。
- 停止录制时不追加“停止按钮点击”。

## P2：编辑器拆分

`EditorPanel` 已经承担太多职责，建议拆成几个子组件：

- `TaskEditor`：任务名称、间隔、次数、窗口、ROI。
- `RuleEditor`：图片、阈值、多实例、分支。
- `ActionEditor`：动作列表、动作详情、坐标拾取。
- `RecorderControls`：录制按钮、轨迹开关、录制状态。
- `BlueprintEditor`：蓝图视图和节点分支编辑。

拆分后好处：

- 单个文件更短。
- 信号连接更清楚。
- 后续加功能不容易互相影响。

## P2：蓝图交互增强

当前蓝图已支持查看、选中、图片预览和分支下拉编辑。后续可以增强为真正的蓝图编辑：

### 1. 拖拽节点排序

允许用户拖动规则节点改变顺序，并同步规则列表顺序。

### 2. 拖线编辑分支

从节点的“成立”或“不成立”端口拖线到另一个规则节点：

- 拖“成立”线：设置 `next_on_found`。
- 拖“不成立”线：设置 `next_on_not_found`。
- 拖到“结束任务”节点：设置结束任务。

### 3. 节点更多信息

节点中显示：

- 图片预览。
- 规则名。
- 阈值。
- 动作数量。
- 分支状态。
- 是否启用。

### 4. 缩放和平移

规则多了以后，需要：

- 鼠标滚轮缩放。
- 中键或空格拖拽平移。
- 一键适配视图。

## P2：性能优化

### 1. 模板图片缓存

当前模板图片匹配时可能重复读盘和解码。建议：

- 在 `TemplateMatcher` 中按图片路径缓存解码后的模板。
- 图片路径或文件修改时间变化时刷新缓存。

### 2. 截图区域优化

尽量引导用户设置 ROI：

- 全屏匹配成本高。
- ROI 越小越快、误匹配越少。
- UI 上可提示“建议设置查找区域”。

## P3：长时间运行体验

### 1. 运行状态面板

显示：

- 当前任务。
- 当前规则。
- 当前任务已执行次数 / 上限。
- 连续失败次数。
- 下一次执行倒计时。

### 2. 运行前检查

点击运行前检查：

- 模板图片是否存在。
- 坐标是否越界。
- 热键是否可用。
- 窗口是否可见。
- 是否存在可能死循环的规则分支。

### 3. 配置备份

每次保存项目时保留最近几份备份：

- `config/backups/project_YYYYMMDD_HHMMSS.json`
- 保留最近 10 份。

## P3：用户体验细节

### 1. 文案统一

统一中文文案，避免中英文混杂：

- `click` 可显示为“点击”。
- `drag` 可显示为“拖拽”。
- `wait` 可显示为“等待”。

底层 JSON 仍保留英文枚举，UI 显示中文。

### 2. 动作预览

在动作列表中更清楚显示：

- 点击：`点击 100,200`
- 拖拽：`拖拽 100,200 -> 300,400`
- 按键：`按键 A`
- 等待：`等待 1000ms`

### 3. ROI 可视化选择

当前 ROI 需要手动输入 `x,y,w,h`。后续可做：

- 截图后框选区域。
- 自动填入 ROI。
- 显示当前 ROI 预览。

## 建议开发顺序

1. 日志落盘。
2. 调度器测试。
3. 配置兼容测试。
4. 模板图片缓存。
5. 窗口检查异常改为安全失败。
6. EditorPanel 拆分。
7. 蓝图拖线编辑。
8. ROI 可视化选择。
9. 运行状态面板。
10. 配置自动备份。

## 当前不建议优先做的事

1. 复杂并发执行：自动化输入并发风险高，容易产生不可控操作。
2. 太早做插件系统：当前项目规模还没到必须插件化。
3. 大规模重写 UI：先拆组件和补交互即可，没必要推倒重来。
4. 高级脚本语言：蓝图和规则分支完善前，不建议引入脚本解释器。

