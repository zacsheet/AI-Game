# 变更记录

本文档用于记录项目代码和配置的修改。后续每次修改代码、脚本或配置时，都需要同步更新本文件。

## 2026-05-23

### 同步启动脚本优化

- 更新 `启动GameBot.bat`。
- 启动目录改为 bat 所在目录，不再依赖硬编码路径。
- 增加 Python 解释器检测逻辑：
  - 优先使用 `.venv\Scripts\python.exe`。
  - 如果 `.venv` 里的 Python 不可运行，则回退检测系统 `python` 和 `py`。
  - 没有可工作的 Python 时输出明确错误提示。
- 没有 `.venv` 时自动创建虚拟环境。
- 依赖缺失时自动执行 `pip install -r requirements.txt`。

### 同步配置调整

- 更新 `config/project.json`。
- 给当前任务前 4 条图片点击动作增加 `delay_after_ms`，降低界面切换未完成就开始下一次匹配的概率：
  - 规则 1：`800ms`
  - 规则 2：`800ms`
  - 规则 3：`800ms`
  - 规则 4：`500ms`

### 同步图片匹配诊断日志

- 更新 `gamebot/core/matcher.py`。
- 新增 `TemplateMatcher.last_score` 和 `TemplateMatcher.last_location`，保存最近一次模板匹配的最高相似度和位置。
- 更新 `gamebot/core/scheduler.py`。
- 当规则未匹配时，日志会输出：
  - 最高相似度
  - 最高相似度位置
  - 当前规则阈值

### 同步单步调试功能

- 更新 `gamebot/ui/app.py`。
- 顶部工具栏新增 `单步` 按钮。
- 连续运行中禁止单步；单步执行中禁止启动连续运行。
- 单步执行放到后台线程中运行，避免阻塞 PyQt 界面。
- 为每个任务记录当前单步位置。
- 每点击一次 `单步`，只执行当前任务的一条规则。
- 本轮规则结束后，下次单步从第一条规则重新开始。
- 更新 `gamebot/core/scheduler.py`。
- 新增 `StepResult`，用于返回单步执行结果和下一条规则索引。
- 新增 `Scheduler.run_rule_once()`，实现逐规则单步执行。
