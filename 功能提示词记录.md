# GameBot 功能提示词记录

本文档记录本轮围绕 GameBot 自动化工具提出的功能需求、交互反馈与修正方向，便于后续继续开发时快速恢复上下文。

## 初始功能需求

1. 通过按下快捷键，移动鼠标即可知道当前屏幕坐标位置，并支持手动复制。
2. 支持编辑指定坐标下的操作。
3. 软件使用过程中支持录制用户操作行为，包括按键、鼠标。
4. 从 A 点移动到 B 点的过程如果没有实际意义，可以只记录点击操作。
5. 用户可通过勾选设置决定是否录制鼠标移动轨迹。
6. 当前编辑功能较复杂，希望优化为更直观的可视化编辑界面，类似蓝图设计。
7. 同一任务中支持设置条件规则，例如规则 B，若规则 A 不成立则执行规则 C，实现类似 if-else 的逻辑控制。
8. 需要评估并实现上述逻辑控制的可行版本。

## 坐标拾取相关提示词

1. 需要快捷键打开坐标拾取窗口。
2. 坐标拾取过程中应实时显示鼠标所在屏幕坐标。
3. 坐标可复制，也可以用于当前动作。
4. 坐标拾取过程中需要快捷键 `C` 支持复制。
5. 关闭也需要快捷键，否则鼠标拖回去点击关闭会看不到坐标值。
6. 当前约定：
   - `F6` 打开坐标拾取。
   - `C` 复制当前坐标。
   - `Enter` 用于当前动作。
   - `Esc` 关闭坐标拾取窗口。

## 录制相关提示词

1. 需要支持录制用户操作行为，包括键盘和鼠标。
2. 默认不录制无意义的鼠标移动轨迹，只记录点击和按键。
3. 勾选“录制鼠标移动轨迹”后，才记录移动点。
4. 需要支持录制拖拽，也就是鼠标按住移动再松开的操作。
5. 录制按钮需要显示快捷键 `(F7)`，否则用户容易忘记。
6. 无法停止录制时，需要修复停止逻辑。
7. 点击“停止录制”按钮本身不应被录制成动作。
8. 录制动作应写入当前选中的规则，而不是每次都写入规则 1。
9. 录制开始时应锁定当前规则，后续录制动作都追加到这个规则下。
10. 当前约定：
    - `F7` 开始/停止录制。
    - 鼠标按下到松开距离超过阈值时记录为 `drag`。
    - 短距离按下松开仍记录为点击。

## 规则与动作编辑相关提示词

1. 动作需要支持图片匹配点和固定坐标两种目标来源。
2. 用户应能手动编辑固定坐标。
3. 录制产生的多个动作需要能查看、选择、编辑和删除。
4. 需要解释“阈值”和“匹配多个实例”的含义：
   - 阈值用于控制图片匹配相似度，越高越严格。
   - 匹配多个实例用于同一图片在屏幕上出现多次时，对多个位置执行动作。

## 蓝图视图相关提示词

1. 编辑界面希望类似蓝图设计，更直观。
2. 蓝图中需要展示规则节点和条件分支。
3. 规则支持“成立后”和“不成立后”的跳转。
4. “成立后”和“不成立后”需要支持“结束任务”，也就是跳出当前任务。
5. 蓝图界面需要有选中态。
6. 在蓝图视角快速点击切换规则时，经常点击不到，需要优化命中区域。
7. 蓝图功能需要能预览配置图片，固定尺寸显示即可。
8. 当前约定：
   - 点击蓝图节点同步选中对应规则。
   - 节点选中时有蓝色描边、浅蓝底色和外圈高亮。
   - 节点内固定尺寸显示模板图片预览。
   - 无图片时显示占位。
   - 蓝图页下方可以编辑“成立后 / 不成立后”的分支目标。

## 任务调度相关提示词

1. 任务需要支持“执行 X 次”的设置。
2. 例如任务 1 只执行 1 次，任务 2 执行 3 次。
3. 任务 1 次数执行完后，需要继续执行任务 2。
4. 如果某个任务设置为不限次数，则会一直执行该任务，不会执行到后续任务。
5. 执行次数达到上限不应永久关闭任务配置，只应在本次运行队列里标记完成。
6. 重新点击运行后，应从头开始按任务队列执行。
7. 任务还需要支持复制，便于快速创建相似任务。
8. 复制任务时应复制规则、动作和分支配置，但生成新的任务 ID 和规则 ID。

## 日志相关提示词

1. 运行日志需要清空功能。
2. 运行日志需要复制功能。
3. 当前已支持复制全部日志和清空 UI 日志。

## 快捷键约定

- `F6`：坐标拾取。
- `F7`：开始/停止录制。
- `F8`：暂停/恢复运行。
- `F9`：停止运行。
- `C`：坐标拾取窗口中复制坐标。
- `Enter`：坐标拾取窗口中将当前坐标用于动作。
- `Esc`：关闭坐标拾取窗口。

